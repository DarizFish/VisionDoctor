# 现场复核

改动应用后，工位重新采集了一次：`run-20260903T042335Z-4fa34448`。

| 工件 | 改动前 | 改动后 |
|---|---|---|
| A | 失败 | 通过 |
| B | 失败 | 通过 |

两件都从失败转为通过。

判定结果是 `not_a_recheck`：这一包的采集时刻早于人工标记的应用时刻，
所以产品不把它算作"现场已恢复"。改动之前采的证据，再好看也不能用来说明改动的效果。
