# 观察证据包

一次运行的全部现场材料，由仿真工位导出。**这是外部世界进入案件的唯一入口**——
产品不从别处取数据，模型也读不到清单之外的任何东西。

- 运行编号：`run-20260903T041431Z-bf9403bc`
- 采集时间：2026-09-03T04:15:46.514061+00:00
- 时钟：`ros_sim_time_and_utc`（质量 `single_container`）
- 代码版本：`992b0f341a971df8acda48160ccf86adbe3b73ad`
- 工件数：51

## 清单构成

| 媒体类型 | 件数 |
|---|---|
| `application/json` | 14 |
| `application/x-ndjson` | 2 |
| `application/x-npy` | 2 |
| `application/yaml` | 2 |
| `image/gif` | 2 |
| `image/png` | 29 |

每件工件在 `bundle.json` 里都记录 `path` / `media_type` / `sha256` / `captured_at` /
`clock_domain`。读取时逐件校验 SHA-256，对不上就拒绝交付。

## 本次运行的任务结果

| 工件 | 判定 | 位置误差 | 旋转误差 | 深度有效比 |
|---|---|---|---|---|
| A | missed_pick_pose | 72.3 mm | 0.0378 rad | 0.688 |
| B | missed_pick_pose | 69.7 mm | 0.0360 rad | 0.688 |

## 修复后重新采集

`run-20260903T042335Z-4fa34448`，同一工位、同一采集方式，唯一变量是被诊断项目的代码版本。

| 工件 | 判定 | 位置误差 |
|---|---|---|
| A | within_tolerance | 1.3 mm |
| B | within_tolerance | 0.6 mm |

## 边界

私有评分材料不在包内。清单只清点公开证据——相机、视频、算法输入输出、日志、配置、
提交号与轨迹。诊断侧拿不到标准答案。
