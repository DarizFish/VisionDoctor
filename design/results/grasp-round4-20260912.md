# 第四轮：有限重复、误判复核与交付检查

状态：本轮全部运行已完成并复核。遮挡复测仍有误诊；后续补正常对照与多观察读取修正见第五轮。

## 条件与评判方式

本轮真实模型均为 `mimo-v2.5-pro`，同一中性提问，不提供干预答案。
诊断实现的 Python 源码指纹为
`37125aacc75c76a3aa9841faca946c89c0f34b89d8e7bcbcd8b6e479d468d953`。
`summary.json` 保存指纹、提问、调用、结论与失败；`cases/CASE-001.json` 保留完整工具返回。
这组运行期间未修改诊断代码。两次深度运行使用同一观察包、不同新案件，没有共享诊断对话。

外部复核由 Codex 对照原始数组、干预代码/记录及完整模型输出完成，不是盲评。
分别评价主要定位、具体机制、排除范围与处理建议，不能把脚本正常退出计作诊断正确。
样本很少，也未测试新工件泛化，不报告通用准确率。

## 原始与消费深度：两次独立诊断

输入：`run-20260912T090840Z-d6384ee9`。同帧 `raw-depth.npy` 与 `depth.npy` 使用相同像素布局，
差异由传感器输出之后、感知之前的受控数据破坏产生。不是材质或真实光学传感失效。

独立读取数组复核：

| 区域 | 原始有效比例 | 消费有效比例 |
|---|---:|---:|
| A 检测矩形 `[323,274,356,300]` | 1.0 | 0.142191 |
| B 检测矩形 `[296,320,309,335]` | 1.0 | 0.0 |

矩形内可能含背景；感知彩色分割区域有效比例为 0，二者不矛盾。
两件均拒绝生成位姿和运动命令。

| 记录（位于 `.runtime/grasp-evaluation/`） | 用时 / 工具 | 复核结论 |
|---|---:|---|
| `round4-depth-a-20260912` | 301.73 秒 / 18 | 定位到原始→消费交接，比较了 A/B，正确区分算法拒绝与自身故障。**部分正确**：成像整体排除过强，对滤波解释偏重 |
| `round4-depth-b-20260912` | 262.58 秒 / 17 | 同样找到数据交接差异，只核对 A 原始 ROI。**部分正确**：把交接直接说成“配准步骤”，具体机制没有证据 |

两次均没有提出代码补丁；建议检查未提供源码的外部采集/处理管线。
该定位有依据，但不能据此批准某个配准修复或参数调整。
输入元数据只声明 sensor_output/program_input，不声明二者之间必有配准。
两次一致找到交接差异，说明新增证据有区分价值；仍不能声称解决了过度归因问题。

## 证据不足对照

`round4-insufficient-final-20260912`：207.58 秒、10 次工具调用，输入
`run-20260912T085751Z-00942e46`，不绑定仓库。

模型保留感知、规划、运动、夹持和配方五类候选，均选择 more_evidence，没有生成补丁，
没有用命令前图像断言之后夹持成功。它仍误选了一次采集 JSON 作为深度参数；
宿主明确返回“此文件不是深度，不能据此推断传感器故障或深度损坏”。最终正确说明深度证据缺失。

按**补证出口有效、报告部分正确**记录：图片观察把工件形状叫错，结论对成像的排除较宽，
补证清单也偏长，没有选择最有区分价值的一项。因此不能写成该案例所有判断都正确。

## 修复后的重复到位

相同模型候选提交 `5dac9d5f92ab4b42c5a47a00d06e6e3b808b4e8d`，两次都重新采集 RGB-D、运行感知和运动。

| 运行 | A 位置误差 | B 位置误差 | 回证 |
|---|---:|---:|---|
| `run-20260912T090708Z-293764aa` | 4.623 mm | 3.503 mm | 应用之后、同来源、完整 A/B，site_recovered |
| `run-20260912T091446Z-9e070b14` | 2.702 mm | 2.493 mm | 应用之后、同来源、完整 A/B，site_recovered |

保存于 `round3-repair-20260912/site-recheck.json` 和 `site-recheck-repeat.json`。
这是指定场景的有限重复，并非抓取成功率统计；只验证到位，没有夹持/抬起物理仿真。

## 遮挡复测：仍未正确辨别物体身份

`round4-physical-review-20260912`：740.88 秒、17 次工具调用。
使用与第三轮相同的遮挡输入；实际调用了带 question 的读图，并检查原图边缘 ROI。

相比第三轮，模型正确识别 A 在图像边缘误选了非目标区域，未再把它当成正确感知优先归咎标定。
但它仍把遮挡板当作白色工件，建议把原蓝色配方改为白色；这会把遮挡物当目标，建议不成立。
它还把同一观察相机的多帧称为多个视角。按**整体误诊、局部定位改善、处理建议错误**记录。
没有执行该建议，没有对参考项目修改颜色配置。

下一步的鉴别证据是同工位同配方的较早正常观察：能核对原工件外观与场景变化。
这是补充独立正常参照，不是把私有遮挡标签提供给模型；在第五轮检验其是否修订判断。

## 检查与边界

- 完整 `pytest -q`：**160 passed in 237.04s**，输出为 `round4-pytest.txt`。
- `ruff check .`、`git diff --check` 通过；换行符提示不表示校验失败。
- 修复后实际案件经内存 API 与 Streamlit AppTest：0 异常、1 个图组件，显示审批与现场复核。
  输出为 `round3-repair-20260912/ui-check.json`，不代表浏览器视觉检查。
- 第三/四轮六个观察包的全部列出文件逐个核对 SHA-256，无不一致，无私有评分/标签路径；
  清单为 `round4-evidence-audit.json`。答案隔离另有既有测试覆盖。
- 主仓库 HEAD 保持 `c562351e778e8ad6a62f02256b855eb203ff0431`；未提交或推送主工作树。

本轮自审：新增证据改善了故障所在交接的辨别，软件修复取得新观察回证；
模型仍会把有依据的局部异常扩张为未经验证的具体机制，视觉身份识别和取证效率也仍有限。
这些限制需要保留在方案和演示中，不用增加工程规模或把每个失败都改成成功来掩盖。

交付审查另复现了同一案件多次观察的同名文件读取错误：接入修复后观察后，取证仍可能用原适配器
读取修复前内容。上面的单观察诊断和直接 bundle 复核不受影响，但后续追问会受影响。
因此本轮测试通过不构成最终交付完成；修正与回归见第五轮记录。
