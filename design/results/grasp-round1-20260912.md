# 第一轮：图与知识接入后的真实模型自审

状态：已检查两次真实模型诊断；根因方向正确，但不能计为完整正确。

## 输入与条件

- 观察：`run-20260903T041431Z-bf9403bc`，历史预设检测位姿路径。
- 源码：`992b0f341a971df8acda48160ccf86adbe3b73ad`。
- 模型：`mimo-v2.5-pro`，真实工具调用，只诊断，不执行修复。
- 原始记录：`.runtime/grasp-evaluation/round1-20260912/` 和
  `.runtime/grasp-evaluation/round1-review-20260912/`。

## 已验证结果

第一次用时 110.44 秒、9 次工具调用。第二次用时 167.97 秒、10 次调用。
两次都指出 `inverse(tool0_to_tcp)` 使用三次、应为一次。第二次实际调用了图查询，
源码读取产生 EV-055～057，可以被结论引用。

第二次原始工具 EV-054 明确返回：

| 参考点/量 | Z 或 Z 差（m） |
|---|---:|
| 期望 TCP | 0.148 |
| 原法兰指令 | 0.253 |
| 使用一次逆工具变换的法兰 | 0.183 |
| 原指令相对上述法兰的额外差 | 0.070 |

## 失败与解释边界

- 两次都混用了补偿总量与错误增量；第二次最终建议把法兰 Z 改到 0.148，仍是错误参考点。
  工具已给出正确值，说明增加数字字段并未消除模型的解释错误。
- 第一次声称检查了 A/B，但相关结论仅引用 A。第二次执行检查缩小到 A，部分范围改善。
- 第二次把 RGB 清晰、全局深度有效比和有检测输出当作成像排除依据。这不足以排除
  目标局部深度问题，也不足以证明感知可靠。
- 第二次称结果误差“本身可信”，却同时承认没有独立验证参考来源；这种排除仍然过强。
- 脚本通过与工具调用成功不能替代对模型结论的人工/外部评分。

## 已采取的下一轮动作

增加目标区域 RGB-D 测量、采集与消费记录对应检查，以及可持久化的计算证据。
明确全局统计、局部表面、工件中心和机器人法兰是不同测量对象。
几何数值解释需要后续真实模型复测；本记录不声明其已修复。
