# 第三轮：多故障场景、真实诊断与修复后回证

状态：已复核本轮实际记录；诊断包含正确方向、部分正确与失败。第四轮复测另记，不能覆盖本轮失败。

## 场景与证据

所有工位运行使用真实 Gazebo 相机、实际 RGB-D 感知和 ROS/MoveIt 路径。
私有干预标签保存在 `.runtime/gazebo-pick-cell/scenario-labels/`，未提供给诊断模型。
以下运行均已导出至 `.runtime/gazebo-pick-cell/exports/`。

| 运行 | 条件与实际结果 |
|---|---|
| `run-20260912T085057Z-f4e53f0e` | 先采图，再把 A/B 沿 X 移动 50 mm，感知继续消费旧图。到位误差 48.759 / 51.447 mm |
| `run-20260912T085331Z-850d5b5d` | 在工件上方加入可见遮挡板。A 误选图像边缘颜色区域并生成不可达目标，B 未检测到目标 |
| `run-20260912T085751Z-00942e46` | 从实际失败运行制作证据不足对照，只提供 A 的 RGB、相机/采集元数据及一般失败报告 |
| `run-20260912T090708Z-293764aa` | 应用模型生成的几何修复后重新采集、感知和执行。A/B 到位误差 4.623 / 3.503 mm，均通过 |
| `run-20260912T090840Z-d6384ee9` | 新深度缺失实验，同时保留原始传感器与程序消费深度；A/B 均拒绝输出位姿和运动命令 |

遮挡是实际场景可见性干预，板未配置碰撞，不能声称测试了实体碰撞或接触。
遮挡同时暴露了颜色感知的误选缺陷，不能把它描述为完全隔离的单故障。
时序与遮挡实验结束后均删除临时板（适用时）并恢复 A/B 位置，服务返回成功；后续正常到位再次提供回证。

## 自审发现的评分错误

第一次遮挡运行 `run-20260912T084910Z-9602fc30` 没有实际法兰测量，但旧评分逻辑用命令值替代了测量，
产生了 1.526954 m 的伪“实际误差”。此值无效，原记录保留为缺陷证据。

已修正：没有实际法兰时记录执行失败，位置与角度误差为 null；不使用命令冒充实际到位。
重新运行得到上表中的 `run-20260912T085331Z-850d5b5d`。测试覆盖了这条区别。

## 真实模型结果与外部复核

模型为 `mimo-v2.5-pro`。以下判断由 Codex 对照干预记录、原始数据与工具返回复核，
不是模型自评，也不是独立盲评。目录前缀为 `.runtime/grasp-evaluation/`。

| 记录 | 用时 / 工具数 | 复核 |
|---|---:|---|
| `round3-repair-20260912` | 217.58 秒 / 19 + 1（两轮） | 软件根因、额外 70 mm 偏差和候选补丁正确；部分排除及反证引用仍不严谨 |
| `round3-timing-20260912` | 19.17 秒 | 远端协议错误，未完成诊断，保留失败 |
| `round3-timing-retry-20260912` | 313.98 秒 / 20 | 正确定位旧帧消费，保留缓存/配置/调用层竞争解释；对感知的排除仍过宽 |
| `round3-physical-20260912` | 583.34 秒 / 19 | **误诊**：把边缘误识别当作正确感知，优先怀疑标定；把不可见误写成工件缺失 |
| `round3-insufficient-review-20260912` | 140.58 秒 / 9 | 正确要求补证，保留后续阶段未知；误把非深度文件的工具错误说成深度格式问题 |

时序案例的对话记录包含“本轮工具预算已用尽”提示，模型随后提交结论。
这是无工具收束分支的真实模型证据；此前第二轮仅有宿主测试。

证据不足案例此前版本 `round3-insufficient-20260912` 用时 99.52 秒，虽然请求补证，
仍从未提交的内部记录推断了失败阶段。随后把结果分类改为中性的 `operator_reported_failure`，
保留原试验，未篡改旧输入。新版本仍存在把“未提供”说成“存在但没给”的表述错误。

## 模型候选与实际恢复

候选来自 `round3-repair-20260912/cases/CASE-001.json` 的 `PLAN-1`，冻结哈希
`7ce43fb913983facd117db24b5f2ffd6ded175ff6068f4ef33710a8a95ec6a36`。
只改 `pick_demo/pipeline.py`，把三次逆工具复合改为一次。

隔离复跑 EV-064 的项目测试为 1 passed，A/B 执行均成功。
独立变换检查 EV-058/059 与候选输出的位置及四元数（考虑 q 与 -q 等价）一致。
A 的法兰 Z 从 0.253660810 m 修正为 0.183660810 m，B 从 0.252625752 m 修正为 0.182625752 m。
期望 TCP 分别为 0.148660810 / 0.147625752 m，未再混用 TCP 与法兰。

原始模型输出仍有问题：把候选成功复跑列为 H1 的反证；部分成像/时序排除表达过强。
候选正确不代表诊断报告每句话都正确。原 EV-064 的 `sources` 为空，输入名与基准提交保存在内容中；
后续代码已补输入证据关联，没有回写旧记录冒充当时已具备。

应用范围仅为 `.runtime/gazebo-pick-cell/projects/repair-evaluation-20260912` 专用实验副本。
通过 CaseService 落地冻结候选，操作者如实记录为 Codex 根据用户已有本地 Demo 实验授权操作，
不是声称获得了一次新的人工点击批准。实验提交：
`5dac9d5f92ab4b42c5a47a00d06e6e3b808b4e8d`，主工作树与正常/故障参考仓库未提交或覆盖。

应用时刻为 `2026-09-12T09:07:06.583829Z`，新观察开始为 `2026-09-12T09:07:08.632012Z`。
新 run ID、同一来源及完整 A/B 集合匹配，`run_recheck` 返回 `site_recovered`。
证据见 `candidate-review.json`、`site-recheck.json`、`case-view-after-recheck.json`。
恢复范围是该工位本次 A/B **到位**，没有物理夹持、抬起或保持验证。

## 本轮改进与下一轮

- 采集阶段进入证据、模型与工作台；未配置视觉模型时不把图片标记为已检查。
- 深度注入保留同帧原始数组，避免只有处理后缺失就归因于材质或传感器。
- 非深度文件传给区域测量时明确报告选择错误，不再暴露易误读的 NumPy pickle 错误。
- 新观察必须在应用之后实际开始，且同来源、完整工件集合匹配才计回证；不把晚导出的旧采集当恢复。
- 读图支持具体鉴别问题，知识与提示强调先核对选中对象，再比较误选、形状假设与标定解释。

最后一组针对性测试 19 passed，Ruff 通过。全量测试与稳定实现上的有限重复运行见第四轮记录。
本轮自审结论：软件修复与现场回证已形成真实闭环，多故障覆盖已扩大；
外部物理原因的视觉归因仍不可靠，需要保留失败并验证改进，不能据此宣布全域诊断成熟。
