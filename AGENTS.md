# 比赛项目协作约束

- 场景固定为视觉引导抓取；当前诊断范围为视觉采集、识别定位、目标计算/规划与机器人到位，故障不限于坐标变换，不扩展接触、夹持。
- 领域知识覆盖工件工艺、光学、采集时序、感知、几何、抓取规划、机械接触、软件接口、测量统计和诊断方法。
- 优先服务比赛核心：诊断效果、可解释证据、Demo 与答辩。略过只在规模化或企业产品化时需要的问题。
- 方案、已实现能力、实际验证范围分别说明；允许方案领先 Demo，不得把规划写成已验证结果。
- 追求可信度和准确率，接受合理失败和证据不足，不以 100% 诊断正确为目标。
- 无兼容性要求；优先让当前主路径清楚、有效，不为旧入口添加兼容层。
- 可按任务需要改造现有仿真工位或构建场景。仿真、故障注入与评分归 harness，产品不得读取故障答案。
- 每轮完成后自审：是否改善核心任务、是否符合当前抓取诊断范围、证据是否支持完成声明、下一步是否明确。
- 项目方向与验收范围见 design/grasp-diagnosis.md；当前状态、验证与自审见 design/PROGRESS.md。
- 有意义的代码改动执行相关测试、pytest 和 ruff check .。真实模型与 Gazebo 必须实际运行才算验证；不推送。
